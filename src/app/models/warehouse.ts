export interface Warehouse {
    createdAt: number[];  // Mảng chứa các giá trị thời gian (mốc thời gian)
    updatedAt: number[];  // Mảng chứa các giá trị thời gian (mốc thời gian)
    id: number;
    name: string;
    location: string;
}
